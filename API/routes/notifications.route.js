import express from "express";
import {
  
} from "../controllers/ligneMo.controller.js";

const router = express.Router();

//creer ligne Achat
router.post("/:blocAffaireId", );

//Update ligne Mo
router.put("/:id", );

// Delete ligne Mo
router.delete("/:id/:blocAffaireId", );

// Get
router.get("/:id", );

//Get All ligne Mo
router.get("/", );

export default router;